---
declared-services: 
  mean-mongo: 
    label: mongolab 
    plan: sandbox 
applications:
- name: passionport
  host: passionport
  memory: 128M
  services:
  - mean-mongo
  env:
    NODE_ENV: cloud-foundry